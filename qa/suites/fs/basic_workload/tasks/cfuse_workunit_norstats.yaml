tasks:
- check-counter:
    counters:
      mds:
        - "mds.dir_split"
- workunit:
    timeout: 3h
    clients:
      all:
        - fs/norstats

overrides:
  ceph:
    conf:
      client:
        client dirsize rbytes: false
